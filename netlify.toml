[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Single-page app: serve index.html for any route so client-side
# navigation (and direct/deep links) always resolve.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
